node_modules/
dist/
dist-electron/
release/
.npm-cache/
*.log
npm-debug.log*
.vite/
.eslintcache
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# 二进制的取舍（重要）
#
# resources/bin/ 下的 ffmpeg.exe + ffprobe.exe 合计约 140 MB，.downloads/ 里的
# 中间产物约 190 MB。这些都不入库，原因：
#   1) GitHub 单文件 100 MB 硬限制、仓库 1 GB 软限制，塞进去会把仓库撑爆
#   2) 二进制不适合走 git 的差分存储，每次替换都会让仓库历史翻倍
#   3) 一行命令就能按需获取：node scripts/fetch-binaries.mjs（走 npm 镜像 + GitHub 代理）
#
# 因此：克隆后请执行 `npm run setup`，它会自动把 ffmpeg/ffprobe 准备好。
# 发布时若需要"开箱即用"的整包，用 `npm run dist:portable` 产出的离线便携版目录
# （release/Lumen-conv-便携版/，不需要联网，只用本机已有的 electron/dist 与 resources/bin）；
# 需要带图标的 NSIS 安装包时在有网络的环境跑 `npm run dist:nsis`（electron-builder 已配置 extraResources）。
# ---------------------------------------------------------------------------
resources/bin/
.downloads/
# 调试/探测脚本的临时打包目录（由测试脚本运行时创建）
.tmp-*/
.tmp-*

# 测试产物与样本（全部由脚本现场合成，不进仓库）
#   npm run samples        生成 test-assets/samples/ 下的真实场景素材
#   npm run smoke          产物写到 test-assets/output/
#   npm run test:real:convert  产物写到 test-assets/output-real/
test-assets/
*.tmp
*.part
