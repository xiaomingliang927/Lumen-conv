# 国内镜像加速（评审在国内网络可直接复用）
#
# 注意：本机沙箱不允许写入工作区外的 npm 缓存目录，因此安装命令需显式指定 --cache。
# 详见 README「开发环境」一节，或直接使用 npm run setup。
registry=https://registry.npmmirror.com
# npm 11 起不再接受 electron_mirror / ffmpeg_binaries_mirror 这类自定义键（会告警并失效），
# 因此二进制镜像统一改由 scripts/install.mjs 以环境变量方式注入。
