# 反馈记录（Lumen-conv 视频格式转换器）

本文件记录这次协作中**人类（项目所有者 / 雇主）给出的反馈与要求**，以及 AI 的响应与落地情况。
同时附一节「待用户确认的问题」，列出目前仍需要人类拍板的事项。

记录原则：只记录真实发生过的反馈。AI 自己的判断与推演不算"反馈"，即便它影响了后续动作。
**开发者 / AI 自测发现的缺陷同样不是用户反馈**，因此单列一行并明确标注来源（"反馈来源"列写明"不是用户反馈"），
不与人类反馈混同。

---

## 1. 反馈与响应记录

> **计数口径**：本文档里的检查项数（`smoke` / `smoke:ui:*` / 便携版自检）都是**最新一次实测**的值，
> 自检项会随功能增加而增长（56 → 60、68 → 74 …）。历史条目里的数字也一并按最新值刷新过，
> 所以它表示的是"这条修复**目前**由多少个检查覆盖"，**不等于**"当时就有这么多项"。
> 需要知道某一轮的原始数字时，看 git 历史里那份文档。


| 时间 / 阶段 | 反馈来源 | 反馈内容 | AI 的响应与落地情况 |
| --- | --- | --- | --- |
| 立项阶段 | 项目所有者（转述线上笔试题目） | 提供线上笔试的 **7 条原始要求**，并明确要求**以此作为本次会话的最高优先级约束**：① 视频信息显示 + 缩略图；② 用 ffmpeg 命令行转换；③ 自主优化；④ 生成会话总结 md 并记录开发者决策与反馈；⑤ 提交 github；⑥ 界面选型必须与"客户端"关联，关联为 0 不予考虑；⑦ 设计目标是给人用，不能做的东西自己都用不明白甚至不测试。<br>**同时要求：先"梳理任务和计划"，而不是直接开始写代码** | 把 7 条要求当作需求基线，逐条拆成可验证的功能点，并在实现完成后逐条回溯。落地情况：<br>① `probe.ts` + `thumbnail.ts`（智能选帧 + 磁盘缓存）<br>② `commands.ts` + `process.ts`，界面可展开查看完整命令行原文<br>③ 12 项自主优化，逐条列出代码位置（见 `README.md`）<br>④ 本次交付的 `docs/SESSION_SUMMARY.md` + `docs/DECISIONS.md` + `docs/FEEDBACK_LOG.md`<br>⑤ 配置远端仓库；实际推送状态见下方"交付方式"行与 `SESSION_SUMMARY.md`<br>⑥ 选 Electron（详见 `DECISIONS.md` D-001），并在 README 中单独举证"为什么这是真桌面应用"<br>⑦ 界面层的可用性投入：拖拽即添加、空状态引导、失败任务不死路、全中文文案、设置页诊断区、深浅色主题 |
| 立项阶段（第二步：任务拆解与计划） |  项目所有者 |  在给出 7 条要求之后，**要求先梳理任务与计划**（把要求拆成可执行的任务、排出先后与验收方式） |  第一步交付的不是代码，而是**任务拆解与阶段计划**：把 7 条要求翻译成可验证的功能点，排出六个阶段 —— ① 环境探测与技术选型 → ② 依赖安装与二进制获取 → ③ 主进程与 ffmpeg 能力层 → ④ 渲染进程与界面 → ⑤ 联调、构建与目录整理 → ⑥ 文档与交付，并为每个阶段写明"完成"的判据。<br>**"能用自动化命令复现"从这一步起就是验收前提**：后来每一轮改动都以 `npm run smoke` / `test:real:convert` / `smoke:ui:*` 的退出码收尾，而不是"我看着没问题"。<br>这条计划也解释了后面几轮的推进方式：用户每在成品上发现一个问题，都先归到某个阶段的能力上，改完再跑一遍全套验证。阶段划分见 `SESSION_SUMMARY.md` 第 2 节（阶段一~九） |
| 选型阶段 | 项目所有者 | 在 AI 给出选型分析（Electron vs Tauri vs PySide6 vs WPF，含本机环境探测结果）之后，**选择了「Electron + Vue 3 + Vite + TypeScript」**，而非 Tauri / PySide6 / WPF | 按此选型落地，未再回退讨论其他三条路线。后续所有实现（进程模型、esbuild 打包主进程、electron-builder 打包、`extraResources` 释放二进制）都建立在这个选型上。备选方案与本机环境探测的对应关系记录在 `DECISIONS.md` 的 D-001 |
| 技术边界确认 | 项目所有者 |  问"**可以用 ffmpeg api 吗**"——即是否应该引入 ffmpeg 的库 / 语言绑定，而不是调用命令行 |  结论：**继续用命令行**，三条理由 —— ① 需求②本身就写明"可以用 ffmpeg 的命令行（不要求接入 ffmpeg api）"；② 命令行把"到底转了什么"变成**可复现、可核对的文本**（界面上能展开完整命令原文，这是需求②最直接的证据）；③ 库绑定会引入与 Electron/Node ABI 强耦合的原生依赖，在受限网络下构建风险高。<br>落地：全项目**只有一处** `child_process` 调用（`electron/ffmpeg/process.ts`），只 spawn 两种进程（ffmpeg 本体、取消时的 `taskkill /T /F`），参数一律走**数组**不做字符串拼接。记录见 `DECISIONS.md` D-005 |
| 交付方式（较早阶段） | 项目所有者 | 选择「**现在就给仓库地址和凭据，全程直接推 GitHub**」的交付方式，并提供仓库 `git@github.com:xiaomingliang927/Lumen-conv.git`（**SSH** 形式） | 配置 `origin` 为该 SSH 地址。因受限环境里 MSYS 版 ssh 报 `couldn't create signal pipe`，按环境限制改用 **Windows 原生 OpenSSH**：`git config core.sshCommand "C:/Windows/System32/OpenSSH/ssh.exe"`。远端仓库原有 README 内容仅一行 `# Lumen-conv`，按约定**重命名为 `README.upstream.md` 保留**（未删除上游文件）。<br>**推送状态（后续更新）**：已完成 —— 源码全部推送到 `main`，并在 `v1.0.0` 发布 Release（含便携版附件，已验证匿名可下载）。过程中 SSH 22 端口不通，先改走 `ssh.github.com:443`，该端点也开始超时后最终切到 **HTTPS**（凭据走 Git Credential Manager 已存的 token）。详见 `SESSION_SUMMARY.md` 4.4 |
| 文档阶段（上一轮） | 项目所有者 | 要求为项目补齐交付文档，并**明确要求"不要编造事实"**：只使用已给出的信息，且必须用 read/grep/glob 读源码核实细节；不确定的地方写"待验证"；并如实报告代码与描述不一致之处 | 逐份核对源码（`package.json`、`shared/`、`electron/`、`renderer/`、`scripts/` 与构建配置），产出交付文档；**并且不只做静态核对，还实际执行了仓库自带的验证命令**。`docs/SESSION_SUMMARY.md` 第 4 节据此区分"已实机验证 / 尚未验证"，第 5 节列出描述与代码不一致或实现有缺口的地方 |
| 文档同步阶段（上一轮） | 项目所有者 | 指出"上一轮文档产出之后代码发生了多处修复与变更"，要求把文档与当前代码对齐；**再次强调先读代码再改文档**，如果代码与描述不符以代码为准并在汇报中指出；并给出变更清单（依赖与二进制来源、GIF 缺陷修复、源码目录改名、图标与界面自检等） | 逐项用 read/grep/glob 核实全部 14 条变更描述，并重跑三条验证命令：`npm run typecheck`（退出码 0）、`npm run smoke`（当时 **56/56**）、`npm run smoke:ui:file`（当时 **49/49**，产出 4 张截图）——这三项的**现值**分别是退出码 0、**77/77**、**68/68**（截图 10 张）。据此把 7 份文档全部对齐。**核实中发现描述与代码/实测不符之处并如实上报**，例如：实测压缩比为 661 KB → 250 KB（**38%**）而非描述里的 36%；低分辨率 GIF 回归产物为 **1609 KB** 而非描述里的 1546 KB。另外查出一条描述里没提到的问题：已有 Electron 实例持单实例锁时 `npm run smoke:ui` 不会刷新截图却不报错。核对期间代码库仍在被另一侧修改（`package.json` 新增脚本、`renderer/App.vue` 补注释），因此本轮所有数字都绑定到核实时的代码状态并在文档中注明 |
| 开发阶段（文档对齐之后） | **开发者 / AI 自测（不是用户反馈）** | 界面自检强化到 `--smoke-convert` 一级、并把断言改成"点击前后引擎队列长度必须 0 → 1"之后，暴露出**一条此前两轮自测都没抓到的严重缺陷**：Vue 响应式代理导致 IPC 结构化克隆失败，「开始转换」按钮静默失效（最先在"改过字幕勾选"这条路径上复现；进一步核对表明旧表达式下**每次点击都会失败**）；同时暴露出 `executeJavaScript` 的 `void el.click(); true` 克隆陷阱（`void` 只丢弃值，DOM 节点仍会被结构化克隆） | 修复 `useStore.ts` 的 `effectiveOptions()`（JSON 往返去响应式）与 `startConversion()` 的 try/catch + 错误 toast；`main.ts` 的自检表达式全部包 IIFE 并加 `evalJs(label, expr)` 标签封装；`App.vue` 增加 `unhandledrejection` / `error` 全局监听并显式 `String()` 化；`notifyDone()` 的 `requestPermission()` 改为 `void … .catch()`；`FileList.vue` 的 `.file-sub` 间距 5px → 6px。修复后 `npm run smoke:ui:full` **83/83 通过、退出码 0**，控制台无任何克隆错误。**这条按要求标注来源：它是开发者自测发现的，不是用户反馈**；完整复盘见 `SESSION_SUMMARY.md` 第 5.1.1 节 |
| 打包阶段（本轮） | **开发者 / AI 自测（不是用户反馈）** | 新增"离线便携版打包"能力后，**只有把打包产物真的跑起来**才暴露出来的两个问题：① **打包态路径 bug**——`app.getAppPath()` 在打包态指向 `...\resources\app.asar`（一个**文件**），自检代码却拿它当目录做 `mkdirSync()`，便携版上直接抛 `ENOTDIR, not a directory`（开发态 83/83 全绿完全掩盖了它）；② **同一次运行里截图互相覆盖**——先截 `queue-done.png`（转换完成后）再截 `queue.png`，于是两个文件是同一张图（sha256 相同），而文档一直宣称"`queue.png` 是空队列" | ① `electron/main.ts` 新增 `smokeBaseDir()`：`app.isPackaged` 时返回 `path.dirname(app.getPath('exe'))`，否则返回 `app.getAppPath()`；**截图目录**与 `--smoke-file=` 的相对路径解析都改用它。修复后便携版自检 **82/82 通过、退出码 0**。<br>② `runSmokeCheck()` 里用 `ranConversion` 判定：跑过 `--smoke-convert` 就**不再写 `queue.png`**，只在它不存在时给提示；正确工作流固定为两步（`npm run smoke:ui:file` → `npm run smoke:ui:full`）。重新生成后 `queue.png`（22,942 字节，空队列，逐字节稳定）与 `queue-done.png`（54–58 KB，有任务，因含「已用时」文本会波动）**哈希不同**。<br>③ 顺带把「应用内转换：队列卡片显示完成与产物大小」从"只打印不断言"改成真断言：状态含「已完成」+ 产物大小含数字 + 存在打开按钮。**这两条按要求标注来源：均为开发者自测发现，不是用户反馈**；记录见 `SESSION_SUMMARY.md` 第 5.1 节第 9-11 条与 `TEST_CASES.md` 的 A8 |
| 界面走查阶段（本轮） | 项目所有者（**真实用户反馈**） | 在真的把应用点了一遍之后提出：**"推荐设置和自定义没区别啊"** | 先**量化**再改：让自检把两种模式"首屏（折叠线以上）实际可见的区块"打印出来，得到改动前两种模式的首屏区块**逐块相同**（`视频信息@106 → 用途@376 → 在哪播@787`），专业参数区块在 **900px 之外**——用户说"没区别"是准确的。根因两条：① 「视频信息」默认展开占约 270px，把「用途」推过首屏中线；② 专业参数挂在面板末尾。<br>落地四项改动：① 视频信息默认收起成一行摘要（`infoOpen`）；② 模式切换下方加一行「当前方案摘要」；③ 专业参数区块整体搬到面板**最上面**（仅自定义模式出现）；④ **取消**"切到自定义就自动展开专业参数"（原为省一次点击，但入口搬到首屏后，自动展开会让首屏全是待填字段、主决策被推到 1000px 外）。<br>证据：`npm run smoke:ui:file` **68/68**、`npm run smoke:ui:full` **83/83**，退出码 0；自检新增断言"自定义模式：专业参数入口出现在首屏"→ 实测 `专业参数@152`；两种模式首屏均打印（推荐 `视频信息@152 / 你要拿去干什么@235 / 在哪播 / 多大体积@646`，自定义 `专业参数@152 / 视频信息@205 / 你要拿去干什么@288 / 在哪播 / 多大体积@699`）。<br>完整决策记录见 `DECISIONS.md` D-018；**改动过程中还踩到一个自己造的坑**（搬模板片段只搬了开标签，`vite build` 直接报 `Element is missing end tag`，`vue-tsc` 也过不了），一并记在 D-018 的"本次改动里踩到的坑" |
| 界面走查阶段（本轮第二轮） | 项目所有者（**真实用户反馈**） | 在成品上实际操作后，针对**自定义模式**提出：**"自定义不要这个推荐，缺少专业性，这个用最初那个版本自己调整更合适"**（配图是「你要拿去干什么」的 8 张用途卡片与"选一个用途就够了，细节我来定"） | 上一轮（D-018）只解决了"两种模式看不出区别"，这一轮是把**自定义模式的定位**改对：它不该有推荐。落地五项改动：① 用途卡片整块在自定义模式下不渲染（连同"已偏离推荐值"提示与用途小贴士）；② 专业参数**常开**，删掉折叠开关与 `advancedOpen` 状态（回到"最初那个版本"直接调参数的形态）；③ 摘要行在自定义模式下显示「自定义参数」；④ **兼容性预检拆成独立区块、两种模式都显示**（它拦的是事实性错误，不能跟着推荐一起消失）；⑤ 区块顺序改由 CSS `order` 控制。<br>新增两条验收断言：「自定义模式：不出现用途推荐卡片」实测 **0 张**、「推荐模式：用途推荐卡片齐全」实测 **8 张**。<br>证据：`smoke:ui:file` **68/68**、`smoke:ui:full` **82/82**、便携版 **82/82**，退出码均 0；对照截图 `mode-recommended.png` / `mode-custom.png`。<br>完整决策与三项代价（自定义模式首屏不再有视频信息 / 偏离提示在该模式下不可见 / 页面更长）见 `DECISIONS.md` D-019 |
| 界面走查阶段（本轮第三轮） | 项目所有者（**真实用户反馈**） | 在成品上继续使用时提出两条：**①"你要拿去干什么和在哪播放/多大体积存在耦合情况"；②"我换成手机的但是屏幕比例没变"** | 两条都先查证再改，且**都发现了比反馈本身更严重的底层问题**：<br>① 表面是耦合，实际是"**语义重叠但没接上**"——「目标体积上限」控件住在「在哪播」里、值却由用途决定（一个参数两个主人）；用途里有「老电视/车载 U 盘」、设备下拉里又有「老安卓电视/车机」，但**选前者根本不会改后者**，于是那个用途最该做的兼容性检查是空的。修法：用途带出设备（8 个用途各自映射）、设备提示写明来源并在手动改过后给"用回用途推荐"、体积上限抽成 `SizeLimitField.vue` 归还用途区（推荐模式）/ 质量区（自定义模式）。<br>② 表面是"比例没变"，实际是"**界面显示了一个它不会产出的分辨率**"——1080p 是上限，实现有"只缩不放"规则，640×360 的源选 1080p 产物仍是 640×360，而界面写着「1080p 全高清」、卡片还写着"按手机屏幕尺寸"。修法：抽出 `shared/output-size.ts` 的 `planOutputSize()` 作为**输出尺寸与 ffmpeg 滤镜的唯一计算处**，界面直接显示「输出尺寸 640×360（源低于 1080p 上限，不放大）」；去掉过度承诺的文案；新增默认关闭的「画面比例」选项（竖屏 9:16 补边 / 裁剪填满）。<br>完整决策与代价见 `DECISIONS.md` D-020 与 D-021 |
| 优化方向征询（本轮第四轮） |  项目所有者（**真实用户反馈**） |  问"还想进行优化帮我给出几个方向"，要求基于**真实缺口**而不是泛泛建议 |  先核对代码确认哪些能力确实不存在（无 CI、无单测框架、无托盘、无命令行接文件、无音频处理、无拼接、无暂停/重排、无自定义预设），再给出 10 个方向 + 5 条"不建议现在做"（NSIS 安装包 / 自动更新 / 代码签名 / i18n / HDR 与 NVENC 实测，各自说明是卡在环境还是收益）。用户从中勾选 6 项：音频处理、字幕烧录、队列控制、Shell 集成、单帧实时预览、便携版瘦身。已落地前 4 项并逐项补了自检与真实素材验证，见 `DECISIONS.md` D-022 ~ D-024 |
| 文档评审轮（本轮第五轮） | 项目所有者（**真实用户反馈**） | 三条具体指令：① 单帧预览"**这个太小了看不清，能不能在中间找个大点的位置显示**"；② "**帮我把便携版作为 github Release 附件**"；③ 会话总结要**补全** —— 把"7 条原始要求作为最高优先级约束""要求先梳理任务和计划"这两件事记进文档，并要求"**写完先给我看一遍，我觉得可以再提交**" | ① 预览从右侧 400px 面板搬到**中间栏下半部分**（两张图各约 190px → 390px，约 4 倍面积），新增**点击放大到整屏**（原图/效果可切换、Esc 关闭），渲染宽度 480 → 960。**这条的教训：第一版从断言角度完全合格（两图都在、参数一变就重渲染），但 190px 里用户根本看不出差别——断言测不出"够不够大"**。见 `DECISIONS.md` D-025 补充。<br>② `v1.0.0` Release 已发布，附件 `Lumen-conv-portable-v1.0.0.zip`（**237.1 MB**），并已用匿名请求验证可下载（HTTP 200 / 大小一致 / 合法 ZIP 魔数）；顺带处理了 GitHub 把中文附件名清洗成 `Lumen-conv-.-v1.0.0.zip` 的问题（API `PATCH` 改名）。<br>③ 本轮**先产出文档供评审、确认后才提交**：`SESSION_SUMMARY.md` 新增第 0 节总览与阶段七/八/九、重写 4.4 交付状态与第 6 节遗留问题；本文件第 3 节刷新（把已完成的移出待办）；并按指令补上立项阶段的任务拆解与计划过程 |

---

## 2. 人类反馈对项目走向的实际影响

把上面几条反馈按"影响程度"重新排一下，可以看出这次协作的决策重心：

| 反馈 | 影响 |
| --- | --- |
| 7 条原始要求 | **决定性的**。其中需求 6（客户端关联）直接决定了技术选型，需求 7（给人用）决定了工作量分配——大量时间花在错误路径、空状态、诊断信息上，而不是堆格式数量 |
| 选定 Electron | **决定性的**。锁定了后续所有架构决策（多进程、IPC 契约、esbuild 打主进程、extraResources 释放二进制） |
| 给仓库与凭据、全程推 GitHub | **流程性的**。改变了工作方式（需要处理 SSH、代理镜像、`.gitignore` 范围），但不影响技术方案 |
| 要求"不要编造事实" | **质量约束**。促使对所有结论做代码核对，并把"未验证"的部分显式标注出来，而不是写成"已完成" |
| "推荐设置和自定义没区别啊" | **一针见血的可用性反馈**。它推翻了"代码上分了模式 = 用户能看出区别"这个假设：两种模式的差异在代码里成立、在**首屏**却不成立。这条反馈把"界面改动是否有效"从主观判断变成可量化断言（打印首屏可见区块），并直接改了四处实现（含一处**反向**修改：取消自动展开）。教训是——**功能存在 ≠ 用户感知到**，验收标准要落在用户第一眼看到的东西上 |
| "自定义不要这个推荐，缺少专业性" | **把模式定位纠正过来的反馈**。上一轮只做到"让差别可见"，这一轮才回答"自定义模式到底该是什么"——答案是**纯参数面板**，不是"推荐模式 + 一个折叠块"。它直接删掉了一整块 UI（用途卡片在自定义模式不渲染）和一层交互（专业参数的折叠开关），并促成了"**兼容性预检必须独立于推荐存活**"这条判断：警告讲的是事实，不能跟着推荐一起消失 |
| "这两块耦合" + "屏幕比例没变" | **两条都指向"界面在说谎"这个共同病根**。第一条暴露的是"一个参数两个主人 + 两块语义重叠却没接线"；第二条暴露的是"界面显示上限、实现却不放大"——用户看到的数字与实际产物不一致。修法都落在同一件事上：**让界面显示的东西与实际执行共用同一份计算**。副产品是一条通用规则：`planOutputSize()` 这类"唯一计算处"应该由 UI 与命令行共同引用，而不是各算各的 |

---

## 3. 待用户确认的问题

> **状态刷新说明**：本节做过多次刷新。**已经做完的事一律移出"待确认"**，
> 免得把"早就完成了"继续挂在待办清单上误导人 —— 下面 3.0 逐条给出证据。
> 最近一次刷新的结论：**原先挂在这里的"是否推送 GitHub""是否把便携版挂 Release""是否补 LICENSE"
> 三项都已经落地**；样本类问题也补齐了大半。真正还开着的只剩 5 条，在 3.1~3.3。

### 3.0 曾经待确认、现已落地

| 原问题 | 结论与证据 |
| --- | --- |
| 是否要完成 GitHub 提交与推送？ | **已完成**。代码已推送到 `main`；`git log` 能看到从首轮实现到发布的分轮提交。推送过程中处理了两个环境问题（SSH 22 端口不通 → 改走 443 端点 → 最终切 HTTPS；中文附件名被 GitHub 清洗 → 用 API 改名），见 `SESSION_SUMMARY.md` 4.4 |
| 是否把便携版挂到 GitHub Release？ | **已完成**。`v1.0.0` 已发布，附件 `Lumen-conv-portable-v1.0.0.zip`（**237.1 MB**，完整便携版 602.4 MB 压缩后）。已验证匿名可下载：HTTP 200、大小一致、前 4 字节是合法 ZIP 魔数。README 顶部也加了直接下载入口 |
| 是否需要补 `LICENSE` 文件？ | **已补**。仓库根目录有 `LICENSE`（MIT），与 `package.json` 的声明一致。另需注意：内置 ffmpeg 是 GPL 构建，**分发物整体按 GPL**，源码按 MIT —— 这一点在 README 的 License 一节写明了 |
| 冒烟测试没覆盖的样本要不要补？ | **补了大半**。字幕（`subs-multi.mkv`，2 条轨）、多音轨（`multi-audio.mkv`，5.1 AC3 + 立体声 AAC）、带旋转元数据的视频（用 `-display_rotation 90` 造出 `rot90.mp4`）都已纳入 `test:real:convert`；**仍未覆盖**：HDR 片源、带片头黑场的视频（见 3.3） |
| 二进制是否入库？ | **已拍板：忽略**。`.gitignore` 排除 `resources/bin/` 与 `.downloads/`，克隆后用 `npm run setup` 获取 |
| 界面自检要不要覆盖"点按钮真的能开始转换"？ | **已覆盖且升级为严格判定**：点击前清空队列，点击后要求引擎队列长度 0 → 1。正是这条断言暴露了"按钮静默失效"的克隆缺陷（见 `TEST_CASES.md` A7） |
| 跨进程传参是否要立规矩？ | **已立规矩**：`DECISIONS.md` D-016 —— 跨 IPC 传参统一走"去响应式的纯对象"（JSON 往返），并写明它丢失 Date/Map 的代价 |

### 3.1 影响验收的事项

1. **是否要在有网络的环境补一个 NSIS 安装包？**
   便携版与精简版都已实测跑通（`release/Lumen-conv-便携版/` 602.4 MB，自检 82/82；
   精简版 282.2 MB，自检 14/14），但 **NSIS 安装包本机做不出来**：
   `electron-builder --win nsis` 需要额外工具链（`app-builder-bin` / `nsis` / `winCodeSign`）
   与一份与 `@electron/get` 不通用的 Electron 缓存，两次实测都以
   `Timeout awaiting 'request' for 600000ms` 失败（见 `SESSION_SUMMARY.md` 4.3 与 `DECISIONS.md` D-017）。
   需要确认：**是否值得换到有网络的环境跑一次 `npm run dist:nsis`**
   （能拿到带图标、带版本信息、带安装向导的正式包），以及由谁执行、在什么环境上验收。

2. **要不要把「精简版」也作为第二个 Release 附件？**
   现在 Release 里只有完整版（237.1 MB）。精简版压缩后约 140 MB，
   对"只想先试试"的人更友好；代价是首次运行要自己指定 ffmpeg 路径。
   目前**没有上传**，因为用户当时只要求传便携版。

### 3.2 内容补充类

3. **是否需要补充演示视频？**
   转码类工具用一段 30 秒的录屏（拖入 → 看信息 → 选用途 → 看预览 → 转换 → 打开产物）
   往往比截图更有说服力。目前**未制作**。

### 3.3 技术细节类

4. **是否在真实 NVIDIA / AMD 机器上验证硬件编码？**
   本机只有 Intel 核显可用（H.264 QSV 实测通过），NVENC 与 AMF 只能做到"探测失败并如实告知"。
   `-cq` / `-b:v` 这些真卡参数没有在任何硬件上跑过 —— 需要确认是否有条件补验。

5. **是否调整界面配色 / 视觉风格？**
   当前是深色为主 + 跟随系统的浅色主题。如果有具体偏好（品牌色、圆角、密度），可以按需调整。

---

## 4. 反馈落实情况一览

| 反馈 | 状态 |
| --- | --- |
| 7 条原始要求 | 6 条已落地（① ② ③ ④ ⑥ ⑦ 有代码依据，其中 ① ② ③ 已由 `npm run smoke` 的 77 项检查实机验证，⑦ 另有界面自检的 83 项断言，含"按钮点击真的创建了任务"与"质量档位真的影响 ffmpeg 命令"），第 ⑤ 条（提交 github）**已执行**（见下表） |
| 选定 Electron 技术栈 | ✅ 已落地（后端逻辑与界面都已实机验证：`smoke` 77/77、`smoke:ui:full` 83/83、退出码 0） |
| 全程直接推 GitHub | ✅ 已执行：`https://github.com/xiaomingliang927/Lumen-conv`（SSH 走 Windows 原生 OpenSSH；HTTPS 亦可直接 clone），共 8 次提交 |
| 指出"点预设卡片右侧的标签没反应"（本轮） | ✅ 已修复，且**问题比反馈的更严重**：右侧标签本就不是按钮，但整张卡片点了也不换高亮——预设选择写进了按文件的 overrides，而选中态读全局。附带修好同类根因的质量/分辨率/帧率/编码器/音频/文件名模板，并新增 4 项预设交互断言 |
| 追问"质量下拉是可以用的吗"（本轮） | ✅ 可用，并补上**硬证据**：切「极小体积」后真实任务的 ffmpeg 命令里出现 `-crf 34`。同时发现它与预设是**同一个根因**的更大面积版本（6 个控件全部写错位置），一并修复 |
| 要求打包到桌面并能直接运行（本轮） | ✅ 已落地：`scripts/package-portable.mjs --shortcut` 用 PowerShell 的 WScript.Shell 创建桌面快捷方式，`Lumen-conv 视频格式转换器.lnk` 指向便携版；实测从快捷方式启动可完成真实转换 |
| "不要编造事实"（上一轮） | ✅ 已执行：全部结论基于源码核对与**实际执行验证命令**，未验证项显式标注 |
| "先读代码再改文档，以代码为准并指出不符"（上一轮） | ✅ 已执行：核实 14 条变更描述并重跑三条验证命令；发现的 3 处数字/事实不符已在 `SESSION_SUMMARY.md` 与本文件第 1 节如实记录 |
| 开发者自测发现的缺陷（**非用户反馈**） | ✅ 已修复并补上防回归断言：克隆缺陷的根因、假通过复盘、断言强化与四条预防措施记录在 `SESSION_SUMMARY.md` 第 5.1.1 节、`TEST_CASES.md` 的 A7、`DECISIONS.md` 的 D-016 |
| 打包阶段自测发现的两个问题（**非用户反馈**，本轮） | ✅ 已修复：① 打包态 `app.getAppPath()` 指向文件导致的 `ENOTDIR` → 新增 `smokeBaseDir()`，便携版自检 **82/82 通过、退出码 0**；② `queue.png` 被 `queue-done.png` 覆盖 → 跑过转换就不再写 `queue.png`，两者现已哈希不同；③ 顺带把队列卡片那条断言从"只打印"升级为真断言。见 `SESSION_SUMMARY.md` 第 5.1 节第 9-11 条、`TEST_CASES.md` 的 A8、`DECISIONS.md` 的 D-017 |
| 优化方向征询（本轮第四轮） | ✅ 已落地：先核对代码确认真实缺口，再给出 10 个方向 + 5 条"不建议现在做"及理由；用户勾选的 6 项中，音频处理 / 字幕烧录 / 队列控制 / Shell 集成 / 单帧预览 / 精简版已全部完成，见 `DECISIONS.md` D-022 ~ D-025 |
| 便携版打包（上一轮新增能力，**非用户反馈**） | ✅ 已落地：`scripts/package-portable.mjs` 离线组装出 `release/Lumen-conv-便携版/`（`Lumen-conv.exe` 200.4 MB、目录 602.4 MB），实跑自检 82/82 通过；**NSIS 安装包仍未产出**，原因是环境限制而非设计取舍（见 `SESSION_SUMMARY.md` 第 4.3 节） |
| "推荐设置和自定义没区别啊"（本轮，**真实用户反馈**） | ✅ 已修复并**量化验证**：先测出两种模式改动前的首屏区块逐块相同，再改四处（视频信息默认收起 / 新增方案摘要 / 专业参数入口搬到首屏最上面 / 取消切模式自动展开）。新增断言"自定义模式：专业参数入口出现在首屏"实测 `专业参数@152`；`smoke:ui:file` **68/68**、`smoke:ui:full` **83/83**，退出码 0。截图 `docs/screenshots/mode-recommended.png` 与 `mode-custom.png` 是同一时刻两种模式的对照（自定义模式顶部多出「专业参数」入口，其余骨架一致）。决策与代价见 `DECISIONS.md` D-018 |
| "自定义不要这个推荐，缺少专业性"（本轮第二轮，**真实用户反馈**） | ✅ 已落地并把定位改对：自定义模式=**纯参数面板**——用途推荐卡片整块不渲染（实测 0 张）、专业参数常开不再折叠、兼容性预检保留且两种模式都显示。新增 2 条专门断言（自定义 0 张卡片 / 推荐 8 张卡片），`smoke:ui:file` **68/68**、`smoke:ui:full` **82/82**、便携版 **82/82**，退出码均 0。决策与三项代价见 `DECISIONS.md` D-019 |
| "用途与在哪播耦合" + "换成手机屏幕比例没变"（本轮第三轮，**真实用户反馈**） | ✅ 两条都已修复，且都找到了比反馈更深的根因：<br>① **用途带出设备**（8 个用途各自映射，选「老电视/车载 U 盘」→ 设备自动变 `android-old`，那个用途的兼容性预检这才真正跑起来）；设备提示写明"由用途自动设定"、手动改过后给「用回用途推荐」；**体积上限**抽成 `SizeLimitField.vue` 归还用途区（推荐模式）/ 质量区（自定义模式），一个参数只有一个主人。<br>② 新增 `shared/output-size.ts` 作为**输出尺寸与 ffmpeg 滤镜的唯一计算处**，界面直接显示「输出尺寸 640×360（源低于 1080p 上限，不放大）」；新增默认关闭的「画面比例」竖屏 9:16 适配（画布短边取 min(上限, 源短边)，竖屏下同样不放大）。<br>证据：`smoke` **77/77**、`test:real:convert` **28/28**（含 3 条 ffprobe 回读真实产物尺寸：4K→720×1280、360p 源→360×640）、`smoke:ui:file` **68/68**、`smoke:ui:full` **82/82**、便携版 **82/82**，退出码均 0 |

> 与本文档配套的还有：`README.md`（项目主文档）、`docs/SESSION_SUMMARY.md`（会话总结与实际验证边界，
> 第 5 节按"已修复 / 仍然存在"列出代码审查发现）、`docs/DECISIONS.md`（26 条决策的备选方案与代价）、
> `docs/CORE_IMPLEMENTATION.md`（核心实现说明）、`docs/TEST_CASES.md`（边界与异常用例）。
